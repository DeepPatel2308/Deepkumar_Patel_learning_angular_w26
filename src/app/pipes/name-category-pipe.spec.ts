import { NameCategoryPipe } from './name-category-pipe';

describe('NameCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new NameCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
